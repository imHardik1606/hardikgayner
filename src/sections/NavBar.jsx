export default function NavBar() {
  return (
    <nav className="sticky top-0 w-full z-50 px-4 py-2 flex items-center justify-center">
      <h2 className="text-2xl font-medium text-primary bg-primary/10 border border-primary/20 px-4 rounded-full">
        Navbar
      </h2>
    </nav>
  );
}
